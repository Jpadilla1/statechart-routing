import React from 'react';
import { StateMachine } from './StateMachine'
import { Routes } from './Routes'

export default () => {
  return (
    <StateMachine>
        <Routes />
    </StateMachine>
  )
}